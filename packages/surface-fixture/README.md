# Dashboard Surface Fixture

Category: `surface_pack`

This fixture represents a surface package that declares read-only context intent for a UI-facing package boundary.

Purpose:

- prove surface-pack category coverage;
- keep UI source outside the metadata fixture;
- avoid runtime mutation authority.
