# Operator Channel Simulator

Category: `channel_adapter`

This fixture represents a channel adapter that can model approval and channel emission capabilities as metadata.

Purpose:

- prove channel-adapter category coverage;
- keep channel behavior behind gateway review;
- avoid executable package entrypoints.
