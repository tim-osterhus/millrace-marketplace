# Local Docs Reader Adapter

Category: `integration_adapter`

This fixture represents an adapter package that can declare a broker-managed credential ID without storing a credential value.

Purpose:

- prove integration metadata parsing;
- prove declared credential references;
- keep adapter source and secrets outside package metadata.
