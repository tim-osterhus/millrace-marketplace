# Memory Notes Fixture

Category: `memory_pack`

This fixture represents a memory-oriented package that declares context and artifact capabilities without storing runtime state.

Purpose:

- prove memory-pack category coverage;
- keep durable state outside package source;
- verify package metadata remains data-only.
